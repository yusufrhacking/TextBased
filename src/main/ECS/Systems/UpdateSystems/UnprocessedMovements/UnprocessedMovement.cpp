#include "UnprocessedMovement.h"

UnprocessedMovement::UnprocessedMovement(Entity entity, double xChange, double yChange) {
    this->entity = entity;
    this->xChange = xChange;
    this->yChange = yChange;
}
